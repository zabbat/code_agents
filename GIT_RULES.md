# Git Rules

- Always ask the user for confirmation before pushing to any remote.
- Before committing, check the current branch. If on `main`, confirm with the user that committing directly to `main` is intended before proceeding.
